

$('.recent-activity-table').html("<%= escape_javascript render 'recent_activity' %>");
Turbolinks.ProgressBar.done();

$('.activity-paginate').click(function (event) {
  Turbolinks.ProgressBar.start();
  Turbolinks.ProgressBar.advanceTo(25);
});
