<div id="container">
  <%= render :partial=>"shared/header" %>
  
  <div>
    <section class="tb">
      <nav class="breadcrumb">
        <a href="<%= vendors_path %>">Certification Dashboard</a> &raquo;
        <span>Edit <%= @vendor.name %></span>
      </nav>
    </section>
    
    <section id="vendor">
      <%= render :partial => 'vendor_details', :locals => { :submit_method => 'put', :submit_text => 'Save' } %>
    </section>
  </div>
</div>

