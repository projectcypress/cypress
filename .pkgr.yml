build_dependencies:
  - mongodb-server
dependencies:
  - mongodb-org-server (= 6.0.4)
  - libcurl4
before:
  - mkdir -p /tmp/db
  - /usr/bin/mongod --dbpath /tmp/db --fork --syslog
targets:
  ubuntu-22.04:
